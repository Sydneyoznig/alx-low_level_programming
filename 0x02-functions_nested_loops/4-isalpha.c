#include <stdio.h>
/**
 * _isalpha - Checks for uppercase
 * Return: 1 if uppercase, 0 otherwise
 * @letter: ASCII code value
 */
int _isalpha(int letter)
{
	if ((letter >= 65 && letter <= 90) || (kletter >= 97 && letter <= 122))
		return (1);
	return (0);
}
